apiVersion: v1
kind: Service

metadata:
  name: cadastro-api-service
  namespace: ecociente

spec:
  type: ClusterIP

  selector:
    app: cadastro-api

  ports:
    - name: http
      protocol: TCP
      port: 9090
      targetPort: 9090